	switch(op){
		case 1: printf("%lf", sum(x, y));
			break;
		case 2: printf("%lf", sub(x, y));
			break;
		case 3: printf("%lf", mul(x, y));
			break;
		case 4:	printf("%lf", div(x, y));
			break;
		return 0;
	}
}
